{
  "name": "myhooks",
  "version": "1.0.4",
  "description": "",
  "main": "dist/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rm -r dist && tsc -d --outDir dist"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/lsert/myHooks"
  },
  "author": "lsert",
  "license": "ISC",
  "devDependencies": {
    "@types/react": "^16.9.52"
  },
  "dependencies": {
    "react": "^16.14.0"
  }
}
